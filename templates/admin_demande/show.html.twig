{% extends 'base.html.twig' %}

{% block title %}Demande{% endblock %}

{% block body %}
    <h1>Demande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demande.id }}</td>
            </tr>
            <tr>
                <th>Creation</th>
                <td>{{ demande.creation ? demande.creation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Precisions</th>
                <td>{{ demande.precisions }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_demande_index') }}">back to list</a>

    <a href="{{ path('admin_demande_edit', {'id': demande.id}) }}">edit</a>

    {{ include('admin_demande/_delete_form.html.twig') }}
{% endblock %}
