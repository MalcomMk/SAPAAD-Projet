{% extends 'base.html.twig' %}

{% block title %}Devis{% endblock %}

{% block body %}
    <h1>Devis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devi.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ devi.prix }}</td>
            </tr>
            <tr>
                <th>Creation</th>
                <td>{{ devi.creation ? devi.creation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_devis_index') }}">back to list</a>

    <a href="{{ path('user_devis_edit', {'id': devi.id}) }}">edit</a>

    {{ include('user_devis/_delete_form.html.twig') }}
{% endblock %}
